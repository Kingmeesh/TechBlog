@font-face {
  font-family: "汉仪正圆55W";
  src: url("/css/fonts/HYZhengYuan-55W.woff2") format("woff2");
}

/* 基础字体和段落样式 */
.markdown-body {
  font-size: 1rem;
  line-height: 1.6;
  font-family: "汉仪正圆55W", sans-serif;
  color: var(--post-text-color);

  p {
    line-height: 1.5;
  }

  strong {
    color: #b71500;
    font-weight: bold;
  }

  /* 标题样式 */
  h1, h2, h3, h4, h5, h6 {
    font-family: "汉仪正圆55W", sans-serif;
    font-weight: bold;
    color: #b71500;
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  h1 {
    font-size: 2rem;
    text-align: center;
  }

  h2 {
    font-size: 1.8rem;
    border-bottom: 2px;
  }

  h3 {
    font-size: 1.4rem;
  }

  h4, h5, h6 {
    font-size: 1.2rem;
  }

  /* 链接样式 */
  a {
    color: #e95f59;
    text-decoration: none;

    &:hover {
      text-decoration: underline;
    }
  }

  /* 块引用样式 */
  blockquote {
    color: #333333;
    background-color: #fdefee;
    border-left: 4px solid #e95f59;
    padding: 10px 16px;
    border-radius: 4px;
    margin: 1rem 0;
  }

  /* 滚动条样式 */
  ::-webkit-scrollbar {
    width: 8px; /* 滚动条宽度 */
    height: 8px; /* 水平滚动条高度 */
  }
  ::-webkit-scrollbar-track {
    background: transparent; /* 轨道透明 */
    border-radius: 10px; /* 可选：轨道圆角 */
  }

  ::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, #e09e9e, #e06b6b); /* 渐变颜色 */
    border-radius: 10px; /* 圆角 */
  }

  ::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(180deg, #d15757, #a64646); /* 悬停时滚动条颜色变化 */
    box-shadow: 0 0 5px rgba(255, 255, 255, 0.3); /* 添加光效 */
  }





  /* 代码块和内联代码 */
  code:not(pre code) {
    font-size: 1.1rem;
    padding: 0.2rem 0.4rem;
    color: #d43200; /* 字体颜色 */
    background-color: transparent;
  }

  /* 普通模式下的代码块样式 */
  pre {
    font-size: 0.9rem !important;
    line-height: 1.5;

    padding: 15px;
    overflow: hidden !important;
    display: block;
    max-width: 100%;
    page-break-inside: avoid;
  }

  pre code {
    font-size: inherit;
    overflow-wrap: break-word;
}


/* 表格样式 */
table {
  tr {
    background-color: var(--board-bg-color);
    transition: background-color .2s ease-in-out;
  }

  tr:nth-child(2n) {
    background-color: var(--board-bg-color);
    transition: background-color .2s ease-in-out;
  }

  th, td {
    border-color: var(--line-color);
    transition: border-color .2s ease-in-out;
  }
}

/* 列表样式 */
ul, ol {
  margin: 1rem 0;
  padding-left: 2rem;
}

li {
  margin-bottom: 0.5rem;
}

/* 分隔线 */
hr {
  display: block !important;
  border: none;
  border-top: 3px solid #d43200;
  margin: 2rem 0;
  background-color: transparent;
}

details {
  cursor: pointer;
}

summary {
  outline: none;
}

/* 图片样式 */
p > img, p > a > img, figure > img, figure > a > img {
  max-width: 90%;
  margin: 1.5rem auto;
  display: block;
  box-shadow: $img-shadow;
  border-radius: 4px;
  background-color: transparent;
}

figcaption.image-caption {
  font-size: .8rem;
  color: var(--post-text-color);
  opacity: 0.65;
  line-height: 1;
  margin: -0.75rem auto 2rem;
  text-align: center;
}

figcaption:not(.image-caption) {
  display: none;
}
}

/* 暗黑模式下的块引用样式 */
html[data-user-color-scheme="dark"] .markdown-body blockquote {
  color: #ffe1e1;
  background-color: #1d2631;
  border-left: 4px solid #e95f59;
}

/* 暗黑模式下的滚动条样式 */
html[data-user-color-scheme="dark"] ::-webkit-scrollbar-track {
  background: transparent;
}

html[data-user-color-scheme="dark"] ::-webkit-scrollbar-thumb {
  background: #f9ebea;
}

html[data-user-color-scheme="dark"] ::-webkit-scrollbar-thumb:hover {
  background: #555;
}

/* 暗黑模式下的代码块样式 */
html[data-user-color-scheme="dark"] pre {
  background-color: #21252b;
  color: #f9f9f9;
}

html[data-user-color-scheme="dark"] .markdown-body {
  h1, h2, h3, h4, h5, h6 {
    color: #be3d38; /* 更亮的红色，提高暗黑模式下的可读性 */
  }

  strong {
    color: #be3d38;
    font-weight: bold;
  }

  h2 {
    border-bottom: 2px solid #ff6b6b; /* 更新边框颜色 */
  }
}
